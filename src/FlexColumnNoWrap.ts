import { styled } from "@mui/system";

export default styled("div")({
    display: "flex",
    flexFlow: "column nowrap",
    alignItems: "center",
    justifyContent: "center",
    margin: "auto",
    padding: "30px",
    maxInlineSize: "400px"
  });